<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.treinamento.mdomingos.startapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.EscolherPlataformaActivity"></activity>
        <activity android:name=".activity.InicialActivity" />
        <activity android:name=".activity.EnviandoEmailActivity" />
        <activity android:name=".activity.SlidesPosCadastroActivity" />
        <activity android:name=".activity.CadastroStartupActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.SobreActivity" />
        <activity android:name=".activity.CadastroUsuarioActivity" />
        <activity
            android:name=".activity.SplashScrean"
            android:label="Entre em sua conta">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>